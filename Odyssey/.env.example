# Copy this file to Odyssey/.env and replace the placeholder locally.
# Odyssey/.env is gitignored. The execution bot never receives this secret;
# it is consumed only by the Minecraft server container's RCON service.
ODYSSEY_RCON_PASSWORD=replace-with-a-long-random-host-secret
