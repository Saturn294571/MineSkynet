# E0 — `e0-executor` 실행 계층 검증

상태: `[~]` 구현·기능·failure-path 검증 완료, commit/tag 고정 대기  
연결 마일스톤: [M0](./M0.md)

## 목표

모델 없이 Minecraft server → Mineflayer bridge → raw skill → inventory/world-state delta 경로를 신뢰할 수 있는 baseline으로 고정한다.

## 고정 환경

- Minecraft `1.19.4`, Fabric Loader `0.15.11`, Java `17.0.15+6`
- Docker image ID `sha256:5b3e96bcd7dace8ab7be89c245dc9ba0b1573fdef2f01d3e101ac40e7843fa70`
- world seed `7634567288700934061`
- Node `20.13.1`, npm `10.5.2`, Mineflayer `4.25.0`
- exact `package-lock.json`과 단일 root dependency tree 사용

## 완료된 검사

- [x] `npm ci`, collectblock TypeScript build, `node --check`, `npm ls --depth=0` 통과
- [x] stale `mineflayer-collectblock/node_modules`에 의존하지 않는 clean install 재현
- [x] `/health`, `/version`과 Minecraft target `1.19.4` 응답 검증
- [x] chunk 준비 전 physics 보류와 finite position/velocity guard 적용
- [x] bot position을 0~30초 동안 5초 간격으로 검사해 7/7 finite 확인
- [x] 매회 hard reset·빈 인벤토리에서 `mineWoodLog` 10/10, log delta `+1`, `onError: []`
- [x] 매회 hard reset·빈 인벤토리에서 `craftCraftingTable` 10/10, table delta `+1`, `onError: []`
- [x] clean `npm ci` 직후 연결·원목·작업대 대표 회귀 재통과
- [x] `/pause`를 호출하지 않는 raw 경로를 확인
- [x] Multiplayer Server Pause·iChunUtil·CompleteConfig·Fabric API가 없는 Fabric server에서 30초 연결·원목·작업대 통과
- [x] `/start`를 request-local bot lifecycle로 변경하고 old/new bot disconnect 경합 제거
- [x] `/step` exception listener를 request-local bot과 response lifecycle에 묶어 누수·`bot=null` crash 제거
- [x] 연속 hard start, 잘못된 port 실패 후 복구, step 오류 후 bridge 생존 회귀 통과
- [x] offline-mode bot UUID를 repository `OPS_FILE`로 고정
- [x] 완전히 새 mod-free server의 첫·두 번째 hard reset과 대표 행동 통과
- [x] 실행별 raw JSON·wall-clock latency·health·dependency·Minecraft log 저장 runner 구현
- [x] runner로 나무 10/10, 작업대 10/10 재통과; 총 20/20, 모든 delta `+1`, `onError: []`

## 남은 작업

- [ ] 성공 commit과 `mineskynet-e0-executor-1.19.4` tag 고정

## 통과 조건

동일 server/world fixture에서 finite position을 유지하고 raw 나무·작업대가 각각 10/10 성공해야 한다. 정상 경로뿐 아니라 reset 실패 뒤에도 bridge가 살아 있어야 하며, 상태 delta·latency·환경 snapshot과 로그 경로가 저장돼야 한다. 이 기능 조건은 통과했으며 commit/tag 고정만 남았다.

상세 실행 결과: [`../E0_test_evidence_2026-08-20.md`](../E0_test_evidence_2026-08-20.md)
